# Dizionario di protocollo

Un documento per versione di protocollo (`kind: protocol`, `templateId` = `ProtocolVer`).
Raccoglie cio' che oggi e' scritto nel codice e nelle tabelle della documentazione:
codifiche non lineari dei periodi (Sampl. T, Trans. T), bit della parola di stato, codici
evento e di allarme.

Previsto dal passo 12 della scaletta, rimandato per decisione.
